import DealsLayout from './deals-layout'

export default function Computing() {
  const topDeals = [];

  return (
    <DealsLayout 
      heading='Top Deals on Computing'
      category='computing'
      // topDeals={topDeals}
    />
  )
}